#include<stdio.h>
#include<string.h>
 int main()
 {
    char firt[]="",second[30]="";
    int i=0,len; 
    printf("enter first string\n");
    gets(first);
    for(i=0;i<=len;i++)
    {
        second[i]='\0'
        printf("concatination of both string is %s ",second);
        retrun 0
    }
 }
